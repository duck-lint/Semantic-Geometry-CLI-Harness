{
  "$schema": "ProjectSpec.schema.json",
  "metadata": {
    "document_id": "project_spec.json",
    "title": "Project Spec",
    "purpose": "Authoritative invariant space for the CLI harness project.",
    "source_format": "json",
    "document_authority": "harness_target"
  },
  "project_thesis": {
    "project_is": [
      "A CLI harness for governed agent-assisted software work.",
      "A system that lets users implement ideas in code through explicit tasks, bounded agent roles, inspectable artifacts, approval gates, and runnable validation probes rather than requiring the user to infer or directly verify code internals.",
      "A repo-local coordination system that turns tasks, role contracts, context packets, runtime policies, reports, run artifacts, and probes into explicit inspectable surfaces.",
      "A harness for making agent participation bounded, reproducible, auditable, and approval-gated rather than chat-shaped or ambient."
    ],
    "project_is_not": {
      "excluded_product_forms": [
        "A chatbot UI.",
        "An IDE extension.",
        "A generic RAG assistant.",
        "A generic autonomous coding agent.",
        "A hidden-memory system."
      ],
      "excluded_agent_patterns": [
        "Free-form multi-agent group chat.",
        "Agents deciding their own tool access.",
        "Agents treating conversational momentum as implementation authority.",
        "Workers mutating repository files without an explicit approval boundary."
      ],
      "design_guardrail": [
        "The harness does not try to make coding agents magical; it makes their claims, inputs, permissions, and evidence inspectable."
      ]
    },
    "project_exists_to": {
      "delineate_boundaries": [
        "Prompt versus policy.",
        "Context versus authority.",
        "Template/example artifact versus live runtime artifact.",
        "Schema contract versus model-generated content.",
        "Planning versus implementation.",
        "Governance role versus file-mutating worker.",
        "Model claim versus deterministic probe evidence.",
        "Active implementation state versus archived evidence."
      ],
      "instantiate_governance": [
        "Compile bounded context packets before model calls.",
        "Load explicit role contracts rather than relying on chat memory.",
        "Validate model outputs against structured report contracts.",
        "Save run artifacts so later review can inspect what actually happened.",
        "Require deterministic probes before treating a slice as runtime-evidenced.",
        "Gate file mutation and worker execution behind explicit approval."
      ]
    },
    "core_runtime_shape": [
      "A CLI command receives a task and selects an explicit workflow route.",
      "The context compiler gathers repo-local authoritative inputs into a project context packet.",
      "The role loader loads the selected role contract and enforces fixed role invariants.",
      "The agent caller sends a bounded model request using the configured provider route.",
      "The returned report is parsed and validated against the runtime output schema.",
      "The run artifact writer saves task, context packet, report, validation summary, and probe evidence under a timestamped run directory.",
      "The orchestrator prints a short truthful terminal summary and stops at the workflow boundary.",
      "Planner, reviewer, adversary, implementer, and archivist routes may be added later only as explicit governed routes.",
      "Worker adapters may mutate repository files only after an approval boundary and must remain separate from governance roles."
    ],
    "primary_operator": "A local developer/operator using model assistance to build software while keeping context, authority, role behavior, tool permissions, and evidence inspectable with no formal coding experience.",
    "highest_value_workflow": {
      "command": "null",
      "args": [
        "null"
      ],
      "expectations": [
        "null"
      ],
      "stop_condition": "null"
    }
  },
  "non_goals": {
    "do_not_build": [
      "A web UI.",
      "A desktop UI.",
      "A VS Code extension.",
      "A vector database or RAG system.",
      "A generic task manager.",
      "Automatic repo mutation from a planning command.",
      "Recursive agent self-improvement.",
      "Provider abstraction beyond the first bounded provider route needed by the current implementation."
    ],
    "do_not_fake": [
      "Runtime capability from scaffolding alone.",
      "Passing status without saved artifacts.",
      "Implementation evidence from a plausible model report.",
      "Known failures, open decisions, or active implementation files as placeholder bureaucracy."
    ],
    "do_not_smooth_over": [
      "Missing files.",
      "Schema/document disagreements.",
      "Contradictions between report status and proof frontier state.",
      "Provider dependency mismatches.",
      "Unclear runtime versus authoring artifact boundaries."
    ],
    "do_not_preserve_without_approval": [
      "Legacy scaffold layouts.",
      "Donor-code assumptions.",
      "Compatibility shims.",
      "Tool permission surfaces that no active role needs."
    ]
  },
  "architectural_shape": {
    "required_runtime_surfaces": [
      "CLI entrypoint.",
      "Deterministic orchestrator.",
      "Task brief writer.",
      "Project context compiler.",
      "Static context packet manifest/reference policy.",
      "Role contract loader.",
      "Provider/model caller boundary.",
      "Structured output parser.",
      "Schema/Pydantic report validator.",
      "Run artifact writer.",
      "Deterministic acceptance probes.",
      "Implementation project state surface.",
      "Worker adapter boundary.",
      "Archive surface."
    ],
    "required_evidence_boundaries": [
      "Role contract versus runtime output schema.",
      "Runtime output schema versus returned report.",
      "Project spec versus current user task.",
      "Invariant authority versus task authority.",
      "Template/example artifact versus live runtime artifact.",
      "Scaffold evidence versus runtime evidence.",
      "Plan versus apply.",
      "Governance agent versus file-mutating worker.",
      "Model-generated claim versus deterministic probe result.",
      "Archived history versus active implementation state."
    ],
    "required_inspectable_surfaces": {
      "default_location": "../runs/<timestamp>/",
      "min_required_artifacts": [
        "task.json",
        "static_context_packet.json",
        "project_manager_report.json"
      ],
      "optional_debug_artifacts": [
        "rendered_request.json",
        "validation_summary.json",
        "probe_summary.json"
      ],
      "authoritative_context_artifact": "static_context_packet.json",
      "authoritative_pm_runtime_output_artifacts": "project_manager_report.json"
    },
    "dependencies": {
      "required": [
        "Python runtime.",
        "Pydantic validation layer.",
        "OpenAI Python SDK v2-compatible route for the first provider path.",
        "OPENAI_API_KEY or equivalent configured outside committed files.",
        "Local filesystem access for repo-local inputs and ../runs/** artifacts.",
        "Local git availability where possible."
      ],
      "excluded": [
        "No external database for the first implementation.",
        "No web search by default.",
        "No shell tool permissions for governance agents.",
        "No committed secrets.",
        "No broad ambient file access by model agents."
      ]
    },
    "runtime_constraints": [
      "The first implementation is Python/JSON first.",
      "The PM route must be useful before worker mutation exists.",
      "The static context compiler must follow the manifest directly: resolve declared sources, enforce declared requiredness and cardinality, parse JSON, and avoid source-specific inference or normalization.",
      "Provider-specific details may exist in the provider/caller layer but should not become role identity invariants."
    ],
    "governance_primitives_constraints": [
      "Object-shaped durable governance should be represented as JSON-first artifacts.",
      "Cross-field invariants should be encoded in validation when contradiction would create routing ambiguity.",
      "Authoring artifacts may carry $schema metadata; runtime outputs should be validated by the runtime contract."
    ],
    "forbidden_surfaces": [
      "Treating archived bundles as active state.",
      "Treating missing optional/template sources as present runtime truth.",
      "Treating example/template files as live runtime inputs.",
      "Claiming runtime capability without a saved runtime artifact and deterministic probe.",
      "Letting a governance role mutate repository source files.",
      "Collapsing report_status and proof_frontier.blocked into the same axis."
    ],
    "deferred_surfaces": [
      "Codex CLI worker adapter execution.",
      "Apply workflow.",
      "Cross-provider abstraction beyond the first model provider path.",
      "UI beyond terminal output.",
      "Long-term cost dashboard."
    ]
  },
  "runtime_discipline": {
    "must_exist": [
      "A task brief is produced from the user task.",
      "project_manager.agent.json loads through the role loader.",
      "static_context_packet.json compiles the manifest-declared JSON sources and records missing required or optional sources truthfully.",
      "The PM call route sends the structured context packet and receives a structured report.",
      "The PM report validates against the report contract and cross-field invariants.",
      "Required run artifacts are saved under a timestamped run directory.",
      "A deterministic probe checks the run artifacts.",
      "The terminal summary reports status, saved path, validation result, and next admissible transformation.",
      "The plan route stops before implementation."
    ],
    "meaningful_progress_metrics": [
      "Schema/model coherence.",
      "Context compiler determinism.",
      "Role contract loading.",
      "Runtime output validation.",
      "Cross-field invariant enforcement.",
      "Run artifact inspectability.",
      "Truthful missing-source handling.",
      "Permission boundary clarity.",
      "Provider dependency alignment.",
      "Acceptance probe reliability."
    ],
    "authoritative_boundaries": {
      "authoritative_sources": [
        "Explicit user request for the current task.",
        "./project_spec/project_spec.json",
        "./project_spec/known_failures.json",
        "./project_spec/open_decisions.json",
        "../runtime/runtime_budget.policy.json",
        "../agents/*.agent.json",
        "../contracts/*.schema.json or generated Pydantic contracts."
      ],
      "authoritative_memory": [
        "../implementations/active/** when an active implementation exists.",
        "../implementations/archive/** for historical evidence.",
        "../runs/** for saved runtime evidence."
      ],
      "agents_must_respect": [
        "Current filesystem state of the repo.",
        "Current git state where available.",
        "The project spec as invariant authority.",
        "Known failures as failure evidence, not roadmap authority.",
        "Open decisions as operational state, not task expansion permission.",
        "Role contracts as role authority.",
        "Runtime budget/policy as execution authority.",
        "Report schemas as output authority.",
        "User approval before file mutation or approval-boundary crossings."
      ]
    }
  },
  "project_quality_bar": {
    "ux_properties": [
      "Boring, legible, bounded CLI.",
      "Obvious route selection.",
      "Obvious run artifact path.",
      "Obvious pass, fail, blocked, or needs-clarification status.",
      "Short terminal summaries with details saved to artifacts.",
      "No surprising mutation.",
      "Readable enough for a novice operator to inspect what happened."
    ],
    "evidence_expectations": [
      "Task text.",
      "Role contract identity.",
      "Context source manifest.",
      "Static context source coverage and missing-source diagnostics.",
      "Model/provider route.",
      "Runtime output schema identity.",
      "Returned report.",
      "Validation result.",
      "Probe result.",
      "Timestamped artifact path."
    ],
    "runtime_expectations": [
      "Context compilation is deterministic and grounded in repo-local sources.",
      "The orchestrator is explicit rather than agent-directed.",
      "The first workflow is hardcoded enough to inspect: task -> compile context -> PM -> validate -> save -> probe -> stop.",
      "Later workflows add roles as explicit routes, not ambient agent autonomy.",
      "Every claimed runtime behavior has a named acceptance probe."
    ],
    "non_trivial_quality_markers": [
      "Not attempted.",
      "Scaffold-only.",
      "Dry-run.",
      "Runtime-evidenced.",
      "Blocked."
    ],
    "operator_expectations": [
      "LLM reports do not substitute for deterministic artifact checks.",
      "The harness should prevent accidental autonomy, hidden context contamination, and non-reviewed file mutation.",
      "The operator may edit JSON configuration, but schemas and validators preserve admissible shape."
    ],
    "inspectability_properties": [
      "Evidence and provenance are visible in saved artifacts.",
      "Failures preserve missing basis rather than smoothing it into prose.",
      "The run can be understood without relying on hidden chat context."
    ]
  },
  "acceptance_probes": [
    {
      "id": "probe-pm-plan-route-saves-artifacts",
      "question": "Can the plan route compile context, call PM, validate the report, save required artifacts, and stop before implementation?",
      "runtime_path_required": {
        "command": "python",
        "args": [
          "-m",
          "harness",
          "plan",
          "Clean up the Pydantic models for the context compiler and PM report validation."
        ]
      },
      "evidence_required": {
        "saved_artifacts": [
          {
            "id": "task",
            "path_pattern": "../runs/<timestamp>/task.json",
            "required": true
          },
          {
            "id": "context_packet",
            "path_pattern": "../runs/<timestamp>/static_context_packet.json",
            "required": true
          },
          {
            "id": "pm_report",
            "path_pattern": "../runs/<timestamp>/project_manager_report.json",
            "required": true
          },
          {
            "id": "validation_or_probe_summary",
            "path_pattern": "../runs/<timestamp>/(validation_summary.json|probe_summary.json)",
            "required": true
          }
        ]
      },
      "not_proof": [
        "A schema file existing on disk.",
        "A mocked report fixture.",
        "A dry-run that does not write artifacts.",
        "A model response that was not validated."
      ]
    },
    {
      "id": "probe-pm-report-cross-field-invariants",
      "question": "Does post-call validation reject a PM report where report_status is admissible but proof_frontier.blocked is true?",
      "runtime_path_required": {
        "command": "python",
        "args": [
          "-m",
          "unittest",
          "tests.test_project_manager_report_validation",
          "-v"
        ]
      },
      "evidence_required": {
        "saved_artifacts": [
          {
            "id": "test_output_or_validation_summary",
            "path_pattern": "../runs/<timestamp>/validation_summary.json",
            "required": false
          }
        ]
      },
      "not_proof": [
        "The fields are present in the schema but can still contradict each other.",
        "Manual review of one example report."
      ]
    },
    {
      "id": "probe-context-compiler-source-boundaries",
      "question": "Does the static context compiler include exactly the manifest-declared JSON sources and report declared required, optional, missing, and cardinality states truthfully?",
      "runtime_path_required": {
        "command": "python",
        "args": [
          "-m",
          "unittest",
          "tests.test_context_compiler",
          "-v"
        ]
      },
      "evidence_required": {
        "saved_artifacts": [
          {
            "id": "compiled_context_packet",
            "path_pattern": "../runs/<timestamp>/static_context_packet.json",
            "required": false
          }
        ]
      },
      "not_proof": [
        "A Python source-contract table that duplicates or overrides the manifest.",
        "Source-specific Pydantic validation or procedural source classification inside the static context compiler.",
        "Placeholder known_failures/open_decisions/implementation files."
      ]
    },
    {
      "id": "probe-no-source-mutation-on-plan-route",
      "question": "Does the plan route avoid repository source mutation while still saving run artifacts?",
      "runtime_path_required": {
        "command": "python",
        "args": [
          "-m",
          "harness",
          "plan",
          "Verify that the PM route stops before implementation."
        ]
      },
      "evidence_required": {
        "saved_artifacts": [
          {
            "id": "run_directory",
            "path_pattern": "../runs/<timestamp>/**",
            "required": true
          }
        ]
      },
      "not_proof": [
        "The PM role instructions say not to mutate files.",
        "No worker adapter exists yet.",
        "A plan was generated without checking git/source changes."
      ]
    }
  ],
  "clarifying_questions": {
    "material_decisions": [
      "Whether implementation plan/tracker should be formalized before planner/reviewer routes are added.",
      "Whether context validation should use generated JSON Schema, Pydantic models, or a deliberate split by artifact type.",
      "Whether budget enforcement belongs in the PM-call slice or the next slice.",
      "Whether planner/reviewer/adversary routes should exist before any worker adapter can mutate files."
    ],
    "probe_sources": "Safe early probe sources are this repository's own JSON, Pydantic, schema, loader, compiler, caller, validator, and run-artifact files.",
    "external_call_privacy_constraints": [
      "Model API calls use billing and must remain bounded.",
      "Secrets must not be committed.",
      "Web search is disabled by default.",
      "Agents do not run shell commands directly.",
      "Agents do not write repository files.",
      "The plan workflow may save run artifacts but must not mutate source files.",
      "Deployment is out of scope."
    ],
    "acceptable_deferrals": [
      "Final packaging.",
      "Cross-provider abstraction.",
      "Multi-agent routing.",
      "Apply workflow.",
      "Codex worker integration.",
      "Long-term cost dashboard.",
      "UI work.",
      "Non-OpenAI model support.",
      "Cross-repo seeding workflow."
    ]
  }
}
